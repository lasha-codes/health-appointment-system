'use client'
import { useDispatch } from 'react-redux'

const AvailableTimesSelector = () => {
  const dispatch = useDispatch()
  return <div>AvailableTimesSelector</div>
}

export default AvailableTimesSelector
