import { service_type } from '@/data/services/services'

const ServiceCard = ({ id, title, image, summary }: service_type) => {
  return <div className='bg-white shadow-sm'></div>
}

export default ServiceCard
